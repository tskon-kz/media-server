# Dev only: where the Vite dev server proxies /api. Point at the dev server's
# bot backend (which must run with WEBAPP_DEV_MODE=1 and port 8081 published via
# docker-compose.dev.yml). Copy to .env.local and set your dev host.
VITE_DEV_API_BASE=http://your-dev-host:8081
